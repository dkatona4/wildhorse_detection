version: "3.9"
services:
  det2:
    build: det2/
    volumes:
      - AI_storage:/AI_storage/
  ind_det:
    build: ind_det/
    volumes:
      - AI_storage:/AI_storage/
      - AI_data:/AI_data

#volumes:
#  datavolume01:
